# ============================================
# Vexorbis Swarm — Environment Variables
# ============================================
# Copy this file to .env and fill in your values.
#   cp .env.example .env

# ============================================
# LLM API Keys (at least one required)
# ============================================
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=                          # Optional fallback
OPENROUTER_API_KEY=                      # Optional

# ============================================
# Git Integration
# ============================================
GITHUB_TOKEN=your_github_personal_access_token  # Required for Git Agent Roles

# ============================================
# Database (Optional — enables session persistence)
# ============================================
# When using Docker Compose, this is pre-configured automatically.
# POSTGRES_URL=postgresql://swarm:swarm-secret@localhost:5433/swarm_memory

# ============================================
# Local LLM Configuration (Ollama / LM Studio)
# ============================================
# Uncomment to use local models instead of cloud APIs
# EMBEDDING_PROVIDER=local
# LOCAL_LLM_URL=http://localhost:11434/v1
# LOCAL_EMBED_MODEL=nomic-embed-text

# ============================================
# Debug / Dev Features (disabled by default in production)
# ============================================
SWARM_DEBUG=false
SWARM_VERBOSE_TELEMETRY=false
SWARM_SBFL_ENABLED=false
SWARM_TRACE_PROMPTS=false

# ============================================
# Security
# ============================================
SWARM_DASHBOARD_KEY=change-me-to-a-strong-secret
SWARM_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8000
